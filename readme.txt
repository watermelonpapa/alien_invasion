激活虚拟环境
source my_env/bin/activate

安装pygame
python3 -m pip install pygame
-m 确保pip这个模块是用python3 这个版本解析。

vscode 里面配置，否则识别不了pygame以及虚拟环境
使用快捷键 Ctrl+Shift+P（或者 Cmd+Shift+P 在 macOS 上）打开命令面板。
输入并选择 Python: Select Interpreter。
在列表中找到并选择你的虚拟环境对应的解释器。通常，这会显示为类似 ./venv 或者包含虚拟环境所在路径的信息。


